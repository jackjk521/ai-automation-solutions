{
  "name": "prompt-forge",
  "version": "1.0.0",
  "description": "Single-file prompt generator + parameterized Markdown template library for Claude Code",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0"
  }
}
